# ── 素材フォルダ ─────────────────────────────────────────────
# 立ち絵・PSD・協賛各社の提供素材など、公開前提でないデータを含みます。
# サイトで使う画像は tools/import-assets.mjs で site/public/assets/ に
# 取り込み済みのため、素材フォルダ自体はリポジトリに含めません。
素材フォルダ/
素材フォルダ*/

# ── OS / エディタ ────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp

# 出演者カードの並び確認用（開発用の中間ファイル）
site/tools/_contact-sheet.png

# 追納素材（元データ。取り込み済みのためリポジトリには含めない）
0830追加分/
